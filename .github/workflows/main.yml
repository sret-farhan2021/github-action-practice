name: Generate podcast feed
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Run feed generator
        uses: sret-farhan2021/dockerized-action-practice@main
